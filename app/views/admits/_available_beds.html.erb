
  <%= hidden_field_tag :date, Date.current  %>
  <%= hidden_field_tag :time, Time.current  %>

  <div class="form-group">
    <label>Select Bed</label>
    <%= select :admit, :bed_id, options_for_select(@available_beds.map {|p| [p.number,p.id]}) %>
    <%#= text_field :admit, :bed_id  %>
  </div>

  <%= submit_tag 'assign', :class => 'btn btn-large btn-primary' %>
